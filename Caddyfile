# FloraSync Local Network Configuration
https://theforge.local:8080, https://localhost:8080 {
    reverse_proxy 127.0.0.1:5050
        tls internal
}

# Note: If you ever buy a real domain name, just replace the top line with `yourdomain.com {` and Caddy will automatically handle the SSL/HTTPS certificates for you!